from typing import Any

from bee_slack_app.model.recommended import Recommended


def recommend(logger: Any, user_id: str) -> Recommended:
    """
    本のレコメンド情報を取得する

    Args:
        user_id : レコメンドを取得したい利用者のuser_id。

    Returns:
        User: 取得したレコメンド情報。取得できない場合は、Noneが返る。
    """
    try:
        # TODO 機械学習の部品が実装できたら、部品を呼び出す。
        Recommended = ml_book_recommend(user)
        print(user_id)
        Recommended["isbn"] = "9784873118253"
        Recommended["recommended_score"] = "3.5"
        return Recommended

    except Exception:  # pylint: disable=broad-except
        logger.exception("Failed to get data.")
        return None
